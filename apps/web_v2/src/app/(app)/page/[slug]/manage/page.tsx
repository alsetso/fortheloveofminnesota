import PageManagePage from '@/features/pages/PageManagePage';

/**
 * /page/:slug/manage — primary listing fields (owner only).
 */
export default function PageSlugManageRoutePage() {
  return <PageManagePage />;
}
