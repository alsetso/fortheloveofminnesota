import ServicesPage from '@/features/services/ServicesPage';

/**
 * /services — home-service bid-request portal (Own scroll surface).
 * Accounts post Marketplace · Wanted pins for work at an address.
 */
export default function ServicesRoutePage() {
  return <ServicesPage />;
}
