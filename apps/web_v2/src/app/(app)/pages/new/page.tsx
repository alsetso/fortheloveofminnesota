import PageLaunchPage from '@/features/pages/PageLaunchPage';

/**
 * /pages/new — create a directory page (Discover + icon, My Pages).
 */
export default function PagesNewRoutePage() {
  return <PageLaunchPage />;
}
