import GovernmentSettingsClient from './GovernmentSettingsClient';

export const metadata = {
  title: 'Government | Settings | Love of Minnesota',
  description: 'Manage your government plan and settings',
};

export default function GovernmentSettingsPage() {
  return <GovernmentSettingsClient />;
}
