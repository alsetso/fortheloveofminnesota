import SettingsBillingPage from '@/features/settings/SettingsBillingPage';

/**
 * /settings/billing — plan, credits, subscription.
 */
export default function SettingsBillingRoutePage() {
  return <SettingsBillingPage />;
}
