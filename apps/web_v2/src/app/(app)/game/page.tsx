/**
 * `/game` — map is mounted by AppShell (`PersistentGameMap`) so Discover can
 * open as a lightbox without remounting Mapbox. This route is a marker only.
 */
export default function GameRoutePage() {
  return null;
}
