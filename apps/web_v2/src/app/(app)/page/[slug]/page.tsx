import PublicPagePage from '@/features/pages/PublicPagePage';

/**
 * /page/:slug — sharable directory page (owner controls when viewer owns it).
 */
export default function PageSlugRoutePage() {
  return <PublicPagePage />;
}
