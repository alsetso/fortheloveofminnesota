import DiscoverPage from '@/features/discover/DiscoverPage';

/**
 * /discover — Own-tab browse (hero + lists) + places/posts search.
 */
export default function DiscoverRoutePage() {
  return <DiscoverPage />;
}
