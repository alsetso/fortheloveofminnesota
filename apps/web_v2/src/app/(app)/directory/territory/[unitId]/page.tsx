import PlacePage from '@/features/place/PlacePage';

/**
 * /directory/territory/:unitId — sharable territory unit record.
 */
export default function DirectoryTerritoryRoutePage() {
  return <PlacePage />;
}
