import PageAdvertisePage from '@/features/pages/PageAdvertisePage';

/**
 * /page/:slug/advertise — page-scoped ads entry (owner only).
 */
export default function PageSlugAdvertiseRoutePage() {
  return <PageAdvertisePage />;
}
