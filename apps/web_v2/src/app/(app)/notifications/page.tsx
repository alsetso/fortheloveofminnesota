import NotificationsPage from '@/features/notifications/NotificationsPage';

/**
 * /notifications — in-app alerts inbox (sidebar → Notifications).
 */
export default function NotificationsRoutePage() {
  return <NotificationsPage />;
}
