import AdsFeedPage from '@/features/ads/AdsFeedPage';

/**
 * /ads — sponsored feed surface (first dedicated ad placement stream).
 */
export default function AdsRoutePage() {
  return <AdsFeedPage />;
}
