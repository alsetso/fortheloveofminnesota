import FeedPage from '@/features/feed/FeedPage';

/**
 * /feed — statewide community posts + pins.
 * Uses the Own tab shell (scroll + account header), not the map.
 */
export default function FeedRoutePage() {
  return <FeedPage />;
}
