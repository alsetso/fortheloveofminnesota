import CalendarPage from '@/features/calendar/CalendarPage';

/**
 * /calendar — community event calendar (Own scroll surface).
 */
export default function CalendarRoutePage() {
  return <CalendarPage />;
}
